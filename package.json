{
  "name": "caravanserai",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "CARAVANSERAI — a trade-world roguelite where every rival merchant is real.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "typecheck": "tsc --noEmit",
    "balance": "vite-node scripts/balance.ts",
    "smoke": "node scripts/smoke.mjs",
    "playtest": "node scripts/playtest.mjs"
  },
  "devDependencies": {
    "jsdom": "^30.1.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vite-node": "^2.1.0"
  }
}
